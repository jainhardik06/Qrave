{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/shared-types/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "nx:noop"
    },
    "build": {
      "executor": "nx:noop"
    },
    "test": {
      "executor": "nx:noop"
    }
  }
}
